import React from 'react';

const Settings = () => {
    return (
        <div className="container" style={{ padding: '2rem' }}>
            <h1 className="text-gradient" style={{ marginBottom: '1rem' }}>Settings</h1>
            <div className="glass-card">
                <p>Manage your account preferences, notification settings, and integrations.</p>
                <p className="text-muted" style={{ marginTop: '1rem' }}>(Placeholder for Settings feature)</p>
            </div>
        </div>
    );
};

export default Settings;
